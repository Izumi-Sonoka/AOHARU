$PKG.Version = "3"

$PKG.Metadata: {
    Version = "3.5a"
    Description = "Terminal multiplexer — manage multiple terminal sessions"
    Homepage = "https://github.com/tmux/tmux"
    Category = "app-misc"
};

$PKG.Depend.BDepends: {
    gcc
    make
    pkgconf
};

$PKG.Depend.RDepends: {
    glibc
    ncurses
    libevent
};

$PKG.Sources: {
    urls = "https://github.com/tmux/tmux/releases/download/3.5a/tmux-3.5a.tar.gz"
};

$PKG.Checksums: {
    sha256:b6af4c2bde3db4ac7cfc90e4b28c7bb0f92c96827e5a3512e2c9dbb23c48a6d5 tmux-3.5a.tar.gz
};

$PKG.Build: {
    cd tmux-3.5a
    ./configure \
        --prefix=/usr \
        --sysconfdir=/etc
    make -j$(nproc)
};

$PKG.Package: {
    cd tmux-3.5a
    make DESTDIR="$PKGDIR" install
    install -Dm644 example_tmux.conf "$PKGDIR/usr/share/doc/tmux/example_tmux.conf"
};
